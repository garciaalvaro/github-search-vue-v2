version: "3.7"

services:
  github-search-vue-v2:
    build: .
    image: nodejs:15
    container_name: github-search-vue-v2
    restart: unless-stopped
    ports:
      - 3000:3000
    volumes:
      - .:/app
